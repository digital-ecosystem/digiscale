{
  "compilerOptions": {
    /* File layout */
    "rootDir": "./src",
    "outDir": "./dist",

    /* Target Node (modern JS but not bleeding edge) */
    "target": "ES2020",
    "module": "commonjs",
    "moduleResolution": "node",

    /* Make default imports like `import express from "express"` work */
    "esModuleInterop": true,

    /* Node types (we installed @types/node already) */
    "types": ["node"],

    /* Useful dev options */
    "strict": true,
    "skipLibCheck": true,
    "sourceMap": true
  },
  "include": ["src"]
}
